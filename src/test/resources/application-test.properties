spring.application.name=d3tec
server.servlet.context-path=/api/v1

# Jwt Configuration (test)
jwt.public.key=classpath:test-app.pub
jwt.private.key=classpath:test-app.key
jwt.token.expires.in=3600

# Setup inicial
bootstrap.admin.email=admin@admin.com
bootstrap.admin.password=admin

# Upload de arquivos (test)
storage.upload-dir=build/uploads-test
